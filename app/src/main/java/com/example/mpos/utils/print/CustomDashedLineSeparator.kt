package com.example.mpos.utils.print
/*

import com.itextpdf.text.pdf.PdfContentByte
import com.itextpdf.text.pdf.draw.DottedLineSeparator


class CustomDashedLineSeparator : DottedLineSeparator() {
    var dash = 5f
    var phase = 2.5f

    override fun draw(
        canvas: PdfContentByte,
        llx: Float, lly: Float, urx: Float, ury: Float, y: Float
    ) {
        canvas.saveState()
        canvas.setLineWidth(lineWidth)
        canvas.setLineDash(dash, gap, phase)
        drawLine(canvas, llx, urx, y)
        canvas.restoreState()
    }
}
*/
